# テスト戦略

## 役割分担

| レベル | 保証すること | 実行タイミング |
| --- | --- | --- |
| E2E | 登録、認証、タスク完了、フィード、マイページなど、ユーザーの重要な導線が実際につながること | PRと`develop`へのpush |
| API・結合 | HTTPステータス、認証・認可、CSRF、データ永続化、404などの異常系 | PRと`develop`へのpush |
| 単体・コンポーネント | 入力値の境界、API応答の変換、モーダル、編集破棄、細かなUI分岐 | PRと`develop`へのpush |

E2Eの本数ではなく、ユーザー影響、不具合時のリスク、保守コストで配置を決める。APIをモックするPlaywrightテストはUI結合テストとして扱い、実バックエンドを通るテストと区別する。

## Playwrightの整理

既存45件を確認し、ブラウザを使わないAPI直接呼び出し4件をRailsリクエストスペックへ集約した。既存41件は代替となるコンポーネントテストがまだないため維持し、実バックエンドの主要シナリオ1件を追加した。Chromiumでは合計42件を実行する。

既存41件の内訳は、認証・登録UI、タスク画面、フィード・マイページのUI結合、ローディング、レスポンシブ表示である。このうち入力制御、モーダル、表示寸法は将来のコンポーネントテスト移行候補で、Cookie・リロード・複数画面の遷移はE2E維持候補とした。

### E2Eとして残す

- `@critical`の主要シナリオ: 新規登録 → ログアウト → ログイン → タスク開始 → タスク完了 → フィード確認 → マイページ確認 → ログアウト
- アカウント削除のように復旧が難しく影響が大きい操作
- Cookie、画面遷移、ブラウザ再読み込みなど、ブラウザとバックエンドの境界でのみ保証できる動作
- ローディングから通常画面への復帰など、ページ全体の状態遷移

ChromiumではPlaywrightテスト全体を実行する。FirefoxとWebKitでは`@critical`だけを実行し、ブラウザ差分を低い保守コストで検出する。

### 別レイヤーへ移す

次の4件はブラウザ操作を伴わずPlaywrightの`request`だけを使用しており、同等以上のRailsリクエストスペックがあるため削除した。

- ヘルスチェック
- APIへのユーザー登録
- メールアドレス重複確認
- APIへのログイン

入力バリデーション、パスワード表示、スマホの細かな寸法、モーダルの開閉などの既存Playwrightテストは、コンポーネントテスト基盤を整えてから段階的に移す。代替テストなしの一括削除はしない。

## API・結合テスト

RailsのRSpecリクエストスペックは、成功時のデータ連携に加えて401、403、404、422、CSRF、他ユーザーのリソース操作拒否を担当する。`backend/test`にはCookieセッション、期限切れ、改ざん、WebSocketトークン失効などの結合テストがあるため、CIでRSpecとMinitestの両方を実行する。

フロントエンドのAPI単体テストは、401や5xx、JSON以外の応答、`fetch`の失敗と表示用エラーへの変換を担当する。実時間を待つタイムアウト試験はE2Eに置かず、AbortSignalを導入する際に偽タイマーを使った単体テストとして追加する。

## 不足テストの優先順位

### P0

- 実バックエンドを通す主要シナリオ（追加済み）
- Firefox・WebKitでの主要シナリオ（CIへ追加済み）
- RailsのMinitest実行漏れ解消（CIへ追加済み）

### P1

- アカウント削除をブラウザから実行し、再ログインできないことを確認するE2E
- 名前・アイコン編集と、保存せず戻る場合のコンポーネントテスト
- 400・500の共通エラー形式とフロント表示のAPI単体テスト
- 長い表示名、長いタスク、長いコメントのコンポーネント境界値テスト

### P2

- 320px、タブレット、デスクトップの代表幅による視覚回帰
- フィード・コメントのページネーションUI
- WebSocket再接続と重複イベント
- キーボード操作とフォーカス遷移

## CI

- Node.js CI: Node 20/22でフロントエンドのビルドと単体テスト
- Ruby on Rails CI: RSpecのAPIリクエストスペック、Rails Minitestのモデル・結合テスト、Lint、セキュリティ検査
- Playwright Chromium: PRと`develop`でPlaywright全体
- Playwright Firefox/WebKit: PRと`develop`で`@critical`のみ
- featureブランチへのpush単体ではPlaywrightを重複起動せず、PRイベントで実行する
